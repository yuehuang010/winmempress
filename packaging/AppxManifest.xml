<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for the Microsoft Store submission.

  Identity/Name, Identity/Publisher and Properties/PublisherDisplayName below
  are development placeholders. tools/package-msix.ps1 overwrites all three in
  the staged copy from its parameters, so the values Partner Center assigns
  never need to be committed here. See store.md.
-->
<Package
    xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
    xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
    xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
    IgnorableNamespaces="uap rescap">

  <Identity
      Name="MemPressMonitor.Development"
      Publisher="CN=MemPressMonitor Development"
      Version="1.0.1.0"
      ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>MemPressMonitor</DisplayName>
    <PublisherDisplayName>MemPressMonitor Development</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="MemPressMonitor" Executable="mempressmonitor.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
          DisplayName="MemPressMonitor"
          Description="See which apps are using your memory and whether memory is actually slowing them down."
          BackgroundColor="#F8FAFC"
          Square150x150Logo="Assets\Square150x150Logo.png"
          Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Wide310x150Logo="Assets\Wide310x150Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <!--
      Required for any Win32 desktop app packaged as MSIX. It is what lets the
      app call OpenProcess / GetProcessMemoryInfo / NtQuerySystemInformation to
      read other processes' memory counters, which is the app's entire purpose.
    -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
